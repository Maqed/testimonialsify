import React from "react";
import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
};

function DashboardPage() {
  return <main className="container">DashboardPage</main>;
}

export default DashboardPage;
